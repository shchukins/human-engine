# Open Decisions

## RAG scope
- should RAG index only docs, or docs + code + GitHub issues?
- should RAG stay inside backend or move to a dedicated service?

## Agent actions
- when should an agent be allowed to create issues or PRs?
- what approval flow is required before write actions?

## AI provider strategy
- local-only
- external API
- hybrid model selection

## Product usage
- which user-facing AI features are actually valuable?
- which ones should remain internal developer tools only?